@using TightWiki.Plugin
@using TightWiki.Plugin.Interfaces
@using TightWiki
@using TightWiki.Models
@using TightWiki.Library
@inject ITwSharedLocalizationText Localizer
@inject TwConfiguration WikiConfiguration

@model TightWiki.Models.ViewModels.File.PageFileRevisionsViewModel
@{
    Layout = "_Layout";
    var sessionState = ViewData["SessionState"] as TightWiki.SessionState ?? throw new Exception("Wiki State Context cannot be null.");
}

<div class="d-flex justify-content-between align-items-start flex-wrap gap-2 mb-3">
    <div>
        <h3 class="mb-1">
            @Localizer["Page File Revisions"]
        </h3>
        <div class="text-muted">
            @Localizer["All changes that have been made to the attached file."]
        </div>
    </div>
</div>

@if (Model.Revisions.Count > 0)
{
    <div class="card shadow-sm">
        <div class="card-body pb-0">
            @TightWiki.Library.PageSelectorGenerator.Generate(Context.Request.QueryString, Model.PaginationPageCount, @class: "mb-3")
        </div>

        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th scope="col" class="text-nowrap">
                            @Localizer["Revision"]
                        </th>
                        <th scope="col">
                            @Localizer["Modified By"]
                        </th>
                        <th scope="col" class="text-nowrap">
                            @Localizer["Modified Date"]
                        </th>
                        <th scope="col" class="text-nowrap">
                            @Localizer["Size"]
                        </th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var h in Model.Revisions)
                    {
                        <tr>
                            <td class="text-nowrap">
                                <a class="text-decoration-none fw-semibold"
                                       href="@WikiConfiguration.BasePath/File/Binary/@Model.PageNavigation/@Model.FileNavigation/@h.FileRevision"
                                       target="_blank"
                                       rel="noopener">
                                    @Html.DisplayTextFor(x => h.FileRevision)
                                </a>
                            </td>

                            <td>
                                <a class="text-decoration-none"
                                       href="@WikiConfiguration.BasePath/Profile/@h.CreatedByUserName/Public">
                                    @Html.DisplayTextFor(x => h.CreatedByUserName)
                                </a>
                            </td>

                            <td class="text-nowrap text-muted">
                                @Html.DisplayTextFor(x => h.CreatedDate)
                            </td>

                            <td class="text-nowrap">
                                @Html.DisplayTextFor(x => h.FriendlySize)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="card-body pt-3">
            @TightWiki.Library.PageSelectorGenerator.Generate(Context.Request.QueryString, Model.PaginationPageCount)
        </div>
    </div>
}
else
{
    <div class="card shadow-sm">
        <div class="card-body text-muted">
            <i class="bi bi-paperclip me-2"></i>
            @Localizer["This file does not have any revisions."]
        </div>
    </div>
}