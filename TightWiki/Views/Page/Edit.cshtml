@using TightWiki.Models
@using static TightWiki.Library.Constants

@using TightWiki.Resources

@model TightWiki.Models.ViewModels.Page.PageEditViewModel
@{
    Layout = "_Layout";
    var sessionState = ViewData["SessionState"] as TightWiki.SessionState ?? throw new Exception("Wiki State Context cannot be null.");
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/6.65.7/codemirror.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
<link rel="stylesheet" href=@Url.Content("~/codemirror/theme/light.css")>
<link rel="stylesheet" href=@Url.Content("~/codemirror/theme/dark.css")>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/6.65.7/addon/scroll/simplescrollbars.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />

<script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/6.65.7/codemirror.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/6.65.7/addon/edit/matchbrackets.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
<script src="@Url.Content("~/codemirror/mode/tightwiki/tightwiki.js")"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/6.65.7/addon/scroll/simplescrollbars.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>

<script src="@Url.Content("~/syntax/highlight.min.js")"></script>

<style type="text/css">
    .CodeMirror {
        border: 1px solid #eee;
    }

    #editorTabs .nav-link.active {
        background-color: lightgreen !important;
        border-color: #dee2e6 #dee2e6 #fff;
    }

    #dropSection {
        width: 100%; /* Makes the section fully responsive */
        height: auto; /* Allow the height to adjust with content */
        min-height: 150px; /* Minimum height to allow space for content */
        border: 2px dashed #007bff;
        border-radius: 5px;
        text-align: center;
        font-size: 18px;
        color: #007bff;
        margin-bottom: 20px;
        padding: 20px;
        background-color: var(--bs-card-bg); /* Uses Bootstrap background color */
        word-wrap: break-word; /* Forces long words to break and wrap */
        white-space: normal; /* Ensures normal text wrapping */
    }

        #dropSection span {
            display: inline-block; /* Ensure the text behaves like inline elements */
            max-width: 100%; /* Restrict the width to 100% of the container */
            line-height: normal; /* Use normal line height for multi-line text */
        }

        #dropSection.dragover {
            background-color: #e9ecef;
            border-color: #007bff;
            color: #007bff;
        }
</style>

<script>
    function addClipboardImagePasteUpload(editor) {
        const inputEl = editor.getInputField();
        inputEl.addEventListener('paste', async (e) => {
        try {
            const dt = e.clipboardData || window.clipboardData;
            if (!dt) return;

            const files = [];

            //Prefer items (for type checking). Fallback to files list.
            if (dt.items && dt.items.length) {
                for (const item of dt.items) {
                    if (item.kind === 'file') {
                        const file = item.getAsFile();
                        if (file && /^image\//i.test(file.type))
                        {
                            files.push(file);
                        }
                    }
                }
            } else if (dt.files && dt.files.length) {
                for (const f of dt.files) {
                    if (f && /^image\//i.test(f.type)) {
                        files.push(f);
                    }
                }
            }

            if (files.length === 0) return; //Let normal text paste continue
            e.preventDefault(); //We found images; stop the browser from pasting base64 content

            //Build FormData the same way our drag/drop uses it
            const formData = new FormData();
            const chosenNames = [];
            let count = 0;

            for (const file of files) {
                const safeName = suggestFilenameForPaste(file, count++);
                formData.append("postedFiles", file, safeName);
                chosenNames.push(safeName);
            }

            const resp = await fetch('@Url.Action("UploadDragDrop", "File")/@sessionState.PageNavigationEscaped', {
                method: 'POST',
                body: formData
            });
            const result = await resp.json();

            //Insert tags at cursor
            const doc = editor.getDoc();
            const cursor = doc.getCursor();
            const tags = chosenNames.map(n => toWikiimageTag(n)).join("\n");
            doc.replaceRange(tags, cursor);

           //Refresh the attachments list
           $("#uploadedFiles").load("@GlobalConfiguration.BasePath/File/PageAttachments/@sessionState.PageNavigationEscaped");

         } catch (err) {
           const uploadStatus = document.getElementById('uploadStatus');
           if (uploadStatus) {
             uploadStatus.innerHTML = `<p>@SharedLocalizer.Static["Unexpected Error"]: ${err.message}</p>`;
           }
           console.error(err);
         }
       });
     }

     //Create a readable filename.
     function suggestFilenameForPaste(file, count) {
       const extFromName = (file.name || '').split('.').pop();
       const extFromType = (file.type || '').split('/')[1];
       const ext = (extFromName && extFromName.length <= 5) ? extFromName : (extFromType || 'png');
       const stamp = new Date().toISOString().replace(/[-:T.Z]/g, '').slice(0, 15);
       return `clip_${stamp}_${count}.${ext}`;
     }

     //Convert filename to TightWiki tag.
     function toWikiimageTag(name) {
       const ext = (name.split('.').pop() || '').toLowerCase();
       const isImage = ['png','jpg','jpeg','gif','bmp','webp','avif','tif','tiff'].includes(ext);
       return isImage ? `##Image("${name}")` : `##File(${name})`;
     }
</script>

<script>
    const emojiApi = '@(GlobalConfiguration.BasePath)/File/AutoCompleteEmoji';
    const editMode = @(Model?.Id > 0 && @sessionState.HoldsPermission(WikiPermission.Create) ? "true" : "false");
    var editor;
    var featureInsertModalTable;
    var insertEmojiModal;
    var insertLinkModal;
    var featureInsertModal;
    var templatesModal;
    var select2Initialized = false;
    let previewRequestInFlight = null;
    let lastPreviewBody = null;
    let initialBodyValue = '';
    let isBodyDirty = false;
    let isSubmitting = false;

    function surroundSelectionWith(open, close) {
        const doc = editor.getDoc();
        var selection = doc.getSelection();

        if (selection == '') {
            selection = 'TEXT';
        }

        doc.replaceSelection(`${open}${selection}${close}`);

        if (!selection) {
            editor.focus();
            return;
        }

        // Select the new text.
        const cursor = doc.getCursor();
        doc.setSelection({
            line: cursor.line,
            ch: cursor.ch - (selection.length + 4)
        }, {
            line: cursor.line,
            ch: cursor.ch
        });
        editor.focus();
    }

    document.addEventListener("DOMContentLoaded", function () {

    insertEmojiModal = new bootstrap.Modal('#insertEmojiModal');
    insertLinkModal = new bootstrap.Modal('#insertLinkModal');
    featureInsertModal = new bootstrap.Modal('#featureInsertModal');
    templatesModal = new bootstrap.Modal('#templatesModal');

    const pageEditForm = document.getElementById('pageEditForm');
    if (pageEditForm) {
        pageEditForm.addEventListener('submit', function () {
            if (editor) {
                editor.save();
            }
            clearBodyDirtyState();
            isSubmitting = true;
        });
    }

    document.getElementById('insertLinkModal').addEventListener('shown.bs.modal', function() {
        if (select2Initialized)
        {
            // Reset the defaults on the modal:
            document.getElementById('insertLinkModalLinkTitle').value = '';
            document.getElementById('linkModePage').checked = true;
            document.getElementById('linkInternalDiv').style.display = 'block';
            document.getElementById('linkExternalDiv').style.display = 'none';
            document.getElementById('insertLinkModalLinkURL').value = '';
            const $select = $('#insertLinkModalLinkPage');
            $select.val(null).trigger('change');
            return;
        }
        $('#insertLinkModalLinkPage').select2({
            dropdownParent: $('#insertLinkModal'),
            placeholder: "@SharedLocalizer.Static["Search"]...",
            minimumInputLength: 0,
            ajax: {
                url: '@GlobalConfiguration.BasePath/Page/AutoCompletePage',
                dataType: 'json',
                delay: 250,
                data: function(params) {
                    return {
                        q: params.term
                    };
                },
                processResults: function(data) {
                    return {
                        results: data.map(item => ({
                            id: item.id,
                            text: item.text
                        }))
                    };
                },
                cache: true
            }
        });
        document.querySelectorAll('input[name="linkMode"]').forEach(radio => {
            radio.addEventListener('change', () => {
                const mode = document.querySelector('input[name="linkMode"]:checked').value;
                if (mode === 'page') {
                    document.getElementById('linkInternalDiv').style.display = 'block';
                    document.getElementById('linkExternalDiv').style.display = 'none';
                } else {
                    document.getElementById('linkInternalDiv').style.display = 'none';
                    document.getElementById('linkExternalDiv').style.display = 'block';
                }
            });
        });
        select2Initialized = true;
    });

    document.getElementById('insertLinkModalInsertButton').addEventListener('click', function () {
        const linkText = document.getElementById('insertLinkModalLinkTitle').value.trim();
        const linkMode = document.querySelector('input[name="linkMode"]:checked')?.value;

        let markdown = '';
        let isValid = true;

        if (linkMode === 'page') {
            const selected = $('#insertLinkModalLinkPage').select2('data')[0];
            if (!selected) {
                alert("@SharedLocalizer.Static["Select a wiki page."]");
                isValid = false;
            } else {

                if(!linkText)
                {
                    markdown = `[[${selected.text}]]`
                }
                else{
                    markdown = `[[${selected.text}, \"${linkText}\"]]`
                }
            }
        } else if (linkMode === 'url') {
            const url = document.getElementById('insertLinkModalLinkURL').value.trim();
            if (!url) {
                alert("@SharedLocalizer.Static["Enter a URL."]");
                isValid = false;
            } else if (!/^https?:\/\//i.test(url)) {
                alert("@SharedLocalizer.Static["URL must start with"] http:// or https://");
                isValid = false;
            } else {

                if(!linkText)
                {
                    markdown = `[[${url}]]`
                }
                else{
                    markdown = `[[${url}, \"${linkText}\"]]`
                }
            }
        } else {
            isValid = false;
        }

        if (!isValid) return;

        const doc = editor.getDoc();
        var selection = doc.getSelection();

        doc.replaceSelection(markdown);

        // Select the new text.
        const cursor = doc.getCursor();
        doc.setSelection({
            line: cursor.line,
            ch: cursor.ch - (markdown.length + 4)
        }, {
            line: cursor.line,
            ch: cursor.ch
        });

        insertLinkModal.hide();
        editor.focus();
    });

        document.getElementById('featureInsertModal').addEventListener('shown.bs.modal', function () {
            const featureInsertModalTableElement = document.getElementById('featureInsertModalTable');
            const featureInsertModalTableData = @Json.Serialize(Model?.FeatureTemplates);

            if(!featureInsertModalTable)
            {
                featureInsertModalTable = new Tabulator(featureInsertModalTableElement, {
                height: "300px",
                data: featureInsertModalTableData,
                layout: "fitDataStretch",
                columns: [
                        {
                            title: "@SharedLocalizer.Static["Insert"]",
                            headerSort: false,
                            hozAlign: "center",
                            formatter: () => "<a class=\"btn btn-success btn-thin rounded-0\">@SharedLocalizer.Static["Insert"]</a>",
                            cellClick: (e, cell) => {
                                const row = cell.getRow().getData();
                                const value = row.templateText.replace(/\r\n/g, "\n");

                                const doc = editor.getDoc();

                                // current selection (single range assumed)
                                const sel = doc.listSelections()[0];
                                const from = sel.from();
                                const to   = sel.to();

                                // replace and then select the inserted text
                                doc.replaceRange(value, from, to);

                                const start = from;
                                const end = doc.posFromIndex(doc.indexFromPos(start) + value.length);
                                doc.setSelection(start, end);

                                featureInsertModal.hide();

                                editor.focus();
                            }
                        },
                        {
                            title: "@SharedLocalizer.Static["Name"]",
                            field: "name",
                            formatter: function (cell) {
                                const data = cell.getRow().getData();
                                const label = cell.getValue() ?? "";
                                const url = data.helpPageNavigation;
                                if (!url) return label;
                                return `<a href="@GlobalConfiguration.BasePath/${url}" target="_blank" rel="noopener noreferrer">${label}</a>`;
                            }
                        },
                        { title: "@SharedLocalizer.Static["Type"]", field: "type" },
                        { field: "templateText", visible: false },
                        { field: "helpPageNavigation", visible: false },
                        { title: "@SharedLocalizer.Static["Description"]", field: "description" }
                    ],
                });

                //Auto size the last column to fit the content.
                featureInsertModalTable.on('renderComplete', () => featureInsertModalTable.getColumn("description")?.setWidth(true));
            }
            else
            {
                //Redraw the table so the columns re-size correctly.
                requestAnimationFrame(() => featureInsertModalTable.redraw(true));
            }

            document.getElementById("featureInsertModalSearch").addEventListener("keyup", function () {
                const searchTerm = this.value;
                featureInsertModalTable.setFilter([
                    [
                        { field: "name", type: "like", value: searchTerm},
                        { field: "type", type: "like", value: searchTerm},
                        { field: "description", type: "like", value: searchTerm},
                    ]
                ]);
            });
        });

        $("#uploadedFiles").load("@GlobalConfiguration.BasePath/File/PageAttachments/@sessionState.PageNavigationEscaped");

        editor = CodeMirror.fromTextArea(document.getElementById("Body"), {
            theme: "@sessionState.UserTheme.EditorTheme",
            lineNumbers: true,
            mode: "text/x-tightwiki",
            matchBrackets: true,
            viewportMargin: Infinity,
            lineWrapping: true,
            scrollbarStyle: "simple",
            inputStyle: "contenteditable", // Enables browser's native spellcheck
            spellcheck: true,              // Explicitly enables spellcheck
            autocorrect: true              // Enables autocorrect
        });

        const editorHeightSelect = document.getElementById('editorHeightSelect');
        const savedEditorHeight = localStorage.getItem('tightwiki.editorHeight') || '50em';

        if (editorHeightSelect) {
            editorHeightSelect.value = savedEditorHeight;
            setEditorHeight(savedEditorHeight);

            editorHeightSelect.addEventListener('change', function () {
                const selectedHeight = this.value;
                setEditorHeight(selectedHeight);
                localStorage.setItem('tightwiki.editorHeight', selectedHeight);
            });
        }
        else {
            setEditorHeight(savedEditorHeight);
        }

        initialBodyValue = getCurrentBodyValue();
        isBodyDirty = false;

        editor.on('change', function () {
            updateBodyDirtyState();
            lastPreviewBody = null;
        });

        if (editMode) {
            const dropSection = document.getElementById('dropSection');
            const fileInput = document.getElementById('fileInput');
            const uploadStatus = document.getElementById('uploadStatus');

            addClipboardImagePasteUpload(editor);

            // Handle dragover event
            dropSection.addEventListener('dragover', (e) => {
                e.preventDefault();
                dropSection.classList.add('dragover');
                dropSection.textContent = "@SharedLocalizer.Static["Drop files here..."]";
            });

            // Handle dragleave event
            dropSection.addEventListener('dragleave', () => {
                dropSection.classList.remove('dragover');
                dropSection.textContent = "@SharedLocalizer.Static["Attach files by dropping them here or by manually selecting them below:"]";
            });

            // Handle drop event
            dropSection.addEventListener('drop', (e) => {
                e.preventDefault();
                dropSection.classList.remove('dragover');
                dropSection.textContent = "@SharedLocalizer.Static["Attach files by dropping them here or by manually selecting them below:"]";
                const files = e.dataTransfer.files;
                handleFileUpload(files);
            });

            // Open file input dialog on click
            dropSection.addEventListener('click', () => {
                fileInput.click();
            });

            // Handle file input change (manual upload)
            fileInput.addEventListener('change', () => {
                const files = fileInput.files;
                handleFileUpload(files);
            });
        }
        else {
            $('#templatesModal .js_use-template').on('click', function(e) {
                e.preventDefault();
                $.ajax($(this).attr('data-tmpurl'), {
                    success: function(data) {
                        editor.getDoc().setValue(data.body);
                    },
                    complete: function() {
                        templatesModal.hide();
                    }
                });
            });
        }

        const previewTabButton = document.getElementById('preview-tab');
        if (previewTabButton) {
            previewTabButton.addEventListener('shown.bs.tab', function () {
                loadPreview();
            });
        }

        // Function to handle file upload
        function handleFileUpload(files) {
            const formData = new FormData();
            for (let i = 0; i < files.length; i++) {
                formData.append('postedFiles', files[i]);
            }

            uploadFiles(formData);
        }

        // Function to upload files using fetch API
        function uploadFiles(formData) {
            fetch('@Url.Action("UploadDragDrop", "File")/@sessionState.PageNavigationEscaped', {
                method: 'POST',
                body: formData
            })
                .then(response => response.json())  // Read the response as JSON
                .then(result => {
                    if (result.success) {
                        uploadStatus.innerHTML = `<p>@SharedLocalizer.Static["Upload successful"] - ${result.message}</p>`;
                    } else {
                        uploadStatus.innerHTML = `<p>@SharedLocalizer.Static["Error"] - ${result.message}</p>`;
                    }
                    $("#uploadedFiles").load("@GlobalConfiguration.BasePath/File/PageAttachments/@sessionState.PageNavigationEscaped");
                })
                .catch(error => {
                    uploadStatus.innerHTML = `<p>@SharedLocalizer.Static["Unexpected Error"]: ${error.message}</p>`;
                });
        }

       function onInsertEmoji(item){
           const markdown = `%%${item.text}%%`;
           const doc = editor.getDoc();
           var selection = doc.getSelection();

           doc.replaceSelection(markdown);

           const cursor = doc.getCursor();
           doc.setSelection({
               line: cursor.line,
               ch: cursor.ch - (markdown.length + 4)
           }, {
               line: cursor.line,
               ch: cursor.ch
           });

           insertEmojiModal.hide();
           editor.focus();
       }

        function debounce(fn, ms=350){ let t; return (...a)=>{ clearTimeout(t); t=setTimeout(()=>fn(...a), ms); }; }

        const imagesTable = new Tabulator("#emojiResultsTable", {
            layout: "fitColumns",
            placeholder: '@SharedLocalizer.Static["Search for more emojis."]',
            ajaxLoader: true,
            ajaxContentType: "json",
            ajaxResponse: function(url, params, resp) { return resp; },
            columns: [
                {
                    title:"Image", field:"text", width:110, headerSort:false, hozAlign:"center",
                    formatter:(cell)=>{
                        const url = cell.getValue();
                        return `<img src="@GlobalConfiguration.BasePath/File/Emoji/${url}" class="img-thumbnail" style="height:32px;max-width:32px;object-fit:cover;">`;
                    }
                },
                {
                    title:"Name", field:"text", minWidth:160, headerSort:false
                },
                {
                    title:"Action", width:110, headerSort:false, hozAlign:"center",
                    formatter: () => `<button type="button" class="btn btn-thin btn-primary">Insert</button>`,
                    cellClick: (e, cell) => onInsertEmoji(cell.getRow().getData())
                },
            ],
        });

        async function searchImages(q) {
            if(!q?.trim()){
                imagesTable.clearData();
                return;
            }
            imagesTable.setData(emojiApi, { q });
       }

       const $search = document.getElementById("insertEmojiModalSearchText");
       const $insertEmojiModalSearchButton = document.getElementById("insertEmojiModalSearchButton");
       const debounced = debounce(()=>searchImages($search.value), 400);

       $search.addEventListener("input", debounced);
       $search.addEventListener("keydown", e=>{ if(e.key==="Enter") searchImages($search.value); });
       $insertEmojiModalSearchButton.addEventListener("click", ()=>searchImages($search.value));

        //insertEmojiModal is about to be shown.
        document.getElementById('insertEmojiModal')?.addEventListener('shown.bs.modal',() => {
            //If the imagesTable has no data, load a default emoji list.
            if(imagesTable.getData().length == 0)
            {
                imagesTable.setData(emojiApi, { q: '' });
            }
            $search.focus();
        });

        setInterval(ping, 60000);
    });

    function getCurrentBodyValue() {
        return editor ? editor.getValue() : (document.getElementById('Body')?.value || '');
    }

    function updateBodyDirtyState() {
        isBodyDirty = getCurrentBodyValue() !== initialBodyValue;
    }

    function clearBodyDirtyState() {
        initialBodyValue = getCurrentBodyValue();
        isBodyDirty = false;
    }

    window.addEventListener('beforeunload', function (e) {
        if (!isSubmitting && isBodyDirty) {
            e.preventDefault();
            e.returnValue = '';
        }
    });

    function setEditorHeight(height) {
        if (!editor) return;

        const wrapper = editor.getWrapperElement();
        wrapper.style.height = height;
        editor.refresh();
    }

    function ping() {
        const spans = document.querySelectorAll('.badge.text-bg-danger');
        fetch('@Url.Action("Ping", "Page")?r=' + Math.random(), { method: 'GET' })
            .then(response => response.json())
            .then(result => {
                if (result.now) {
                    spans[0].classList.add("invisible");
                    spans[1].classList.add("invisible");
                }
                else {
                    spans[0].classList.remove("invisible");
                    spans[1].classList.remove("invisible");
                }
            })
            .catch(error => {
                spans[0].classList.remove("invisible");
                spans[1].classList.remove("invisible");
            });
    }

    async function loadPreview() {
        const previewContent = document.getElementById('previewContent');
        const previewLoading = document.getElementById('previewLoading');
        const previewError = document.getElementById('previewError');

        previewError.classList.add('d-none');
        previewError.textContent = '';

        const body = editor ? editor.getValue() : document.getElementById('Body').value;
        const pageName = document.getElementById('Name')?.value || '';

        if (body === lastPreviewBody) {
            return;
        }

        lastPreviewBody = body;

        previewLoading.classList.remove('d-none');
        previewContent.innerHTML = '';

        try {
            const token = document.querySelector('input[name="__RequestVerificationToken"]')?.value;

            const response = await fetch('@Url.Action("Preview", "Page")', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'RequestVerificationToken': token
                },
                body: JSON.stringify({
                    body: body,
                    name: pageName,
                    pageNavigation: '@sessionState.PageNavigation'
                })
            });

            if (!response.ok) {
                throw new Error('HTTP ' + response.status);
            }

            const result = await response.json();

            if (result.success) {
                previewContent.innerHTML = result.html;
            } else {
                previewError.textContent = result.message || '@SharedLocalizer.Static["Failed to load preview."]';
                previewError.classList.remove('d-none');
            }
        } catch (err) {
            previewError.textContent = '@SharedLocalizer.Static["Unexpected Error"]' + ': ' + err.message;
            previewError.classList.remove('d-none');
        } finally {
            previewLoading.classList.add('d-none');
            hljs.highlightAll();
        }
    }
</script>

<div class="bodyDiv">
    <div class="card shadow-lg">
        <div class="card-header"><h3>@SharedLocalizer.Static["Content"]</h3></div>
        <div class="card-body">
            @using (Html.BeginForm(FormMethod.Post, new { id = "pageEditForm" }))
            {
                @Html.HiddenFor(x => x.Id)

                <div class="form-group">
                    <button type="submit" class="btn btn-primary">@SharedLocalizer.Static["Save"]</button>
                    @if (sessionState.HoldsPermission(WikiPermission.Read))
                    {
                        if (Model?.Id > 0)
                        {
                            <a href="@GlobalConfiguration.BasePath/@sessionState.PageNavigation" target="_blank" rel="noopener" class="btn btn-success" role="button">@SharedLocalizer.Static["View"]</a>
                            <a href="@GlobalConfiguration.BasePath/@sessionState.PageNavigation/Export" target="_blank" rel="noopener" class="btn btn-secondary" role="button">@SharedLocalizer.Static["Export"]</a>
                        }
                        else
                        {
                            <button type="button" class="btn btn-link" data-bs-toggle="modal" data-bs-target="#templatesModal">@SharedLocalizer.Static["Choose template"]</button>
                        }
                    }
                    <span class="badge text-bg-danger invisible">@SharedLocalizer.Static["Unauthorized"]</span>
                </div>
                <br />

                <div class="mb-3">
                    <label asp-for="Name" class="form-label fw-semibold">@SharedLocalizer.Static["Name"]</label>
                    @Html.TextBoxFor(x => x.Name, new { @class = "form-control" })
                    <div class="text-danger small">
                        @Html.ValidationMessageFor(m => m.Name)
                    </div>
                </div>

                <div class="mb-3">
                    <label asp-for="Description" class="form-label fw-semibold">@SharedLocalizer.Static["Description"]</label>
                    @Html.TextBoxFor(x => x.Description, new { @class = "form-control" })
                    <div class="text-danger small">
                        @Html.ValidationMessageFor(m => m.Description)
                    </div>
                </div>

                @if (GlobalConfiguration.ShowChangeSummaryWhenEditing)
                {
                    <div class="mb-3">
                        <label asp-for="ChangeSummary" class="form-label fw-semibold">@SharedLocalizer.Static["Change Summary"]</label>
                        @Html.TextAreaFor(x => x.ChangeSummary, new
                        {
                            @class = "form-control",
                            rows = 3,
                            placeholder = @SharedLocalizer.Static["What did you change and why?"].Value
                        })
                        <div class="text-danger small">
                            @Html.ValidationMessageFor(m => m.ChangeSummary)
                        </div>
                    </div>
                }
                <!-- EDITOR TABS BEGIN -->

                <div class="d-flex justify-content-between align-items-center mb-2">
                    <ul class="nav nav-tabs" id="editorTabs" role="tablist">
                        <li class="nav-item" role="presentation">
                            <button class="nav-link active"
                                    id="edit-tab"
                                    data-bs-toggle="tab"
                                    data-bs-target="#edit-tab-pane"
                                    type="button"
                                    role="tab"
                                    aria-controls="edit-tab-pane"
                                    aria-selected="true">
                                @SharedLocalizer.Static["Edit"]
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link"
                                    id="preview-tab"
                                    data-bs-toggle="tab"
                                    data-bs-target="#preview-tab-pane"
                                    type="button"
                                    role="tab"
                                    aria-controls="preview-tab-pane"
                                    aria-selected="false">
                                @SharedLocalizer.Static["Preview"]
                            </button>
                        </li>
                    </ul>

                    <div class="d-flex align-items-center gap-2">
                        <label asp-for="editorHeightSelect" class="form-label fw-semibold">@SharedLocalizer.Static["Editor Size"]</label>
                        <select id="editorHeightSelect" class="form-select form-select-sm" style="width:auto;">
                            <option value="10em">@SharedLocalizer.Static["Extra Small"]</option>
                            <option value="20em">@SharedLocalizer.Static["Small"]</option>
                            <option value="30em" selected>@SharedLocalizer.Static["Medium"]</option>
                            <option value="40em">@SharedLocalizer.Static["Large"]</option>
                            <option value="50em">@SharedLocalizer.Static["Extra Large"]</option>
                            <option value="80em">@SharedLocalizer.Static["Huge"]</option>
                        </select>
                    </div>
                </div>

                <div class="tab-content" id="editorTabContent">
                    <div class="tab-pane fade show active"
                         id="edit-tab-pane"
                         role="tabpanel"
                         aria-labelledby="edit-tab"
                         tabindex="0">
                        <div class="text-danger">@Html.ValidationMessageFor(m => m.Body)</div>

                        <!-- Toolbar -->
                        @if (@sessionState.UserTheme.EditorTheme == "dark")
                        {
                            <div class="d-flex gap-2 mb-2">
                                <button type="button" class="btn btn-sm btn-dark p-1" onclick="surroundSelectionWith('**','**')">
                                    <img src="/images/toolbar/toolbar_Dark_Bold.png" width="16" height="16" alt="@SharedLocalizer.Static["Bold"]">
                                </button>
                                <button type="button" class="btn btn-sm btn-dark p-1" onclick="surroundSelectionWith('//','//')">
                                    <img src="/images/toolbar/toolbar_Dark_Italic.png" width="16" height="16" alt="@SharedLocalizer.Static["Italic"]">
                                </button>
                                <button type="button" class="btn btn-sm btn-dark p-1" onclick="surroundSelectionWith('__','__')">
                                    <img src="/images/toolbar/toolbar_Dark_Underline.png" width="16" height="16" alt="@SharedLocalizer.Static["Underline"]">
                                </button>
                                <button type="button" class="btn btn-sm btn-dark p-1" onclick="surroundSelectionWith('~~','~~')">
                                    <img src="/images/toolbar/toolbar_Dark_Strike.png" width="16" height="16" alt="@SharedLocalizer.Static["Strikethrough"]">
                                </button>
                                <button type="button" class="btn btn-sm btn-dark p-1" onclick="surroundSelectionWith('!!','!!')">
                                    <img src="/images/toolbar/toolbar_Dark_Highlight.png" width="16" height="16" alt="@SharedLocalizer.Static["Highlight"]">
                                </button>
                                <button type="button" class="btn btn-sm btn-dark d-flex align-items-center gap-1 px-2" data-bs-toggle="modal" data-bs-target="#insertLinkModal">
                                    <img src="/images/toolbar/toolbar_Dark_Link.png" width="16" height="16" alt="@SharedLocalizer.Static["Hyperlink"]...">
                                </button>
                                <button type="button" class="btn btn-sm btn-dark d-flex align-items-center gap-1 px-2" data-bs-toggle="modal" data-bs-target="#insertEmojiModal">
                                    <img src="/images/toolbar/toolbar_Dark_Emoji.png" width="16" height="16" alt="@SharedLocalizer.Static["Emoji"]...">
                                </button>

                                <div style="width:1px; height:24px; background-color:#ccc;"></div>

                                <button type="button" class="btn btn-sm btn-dark d-flex align-items-center gap-1 px-2" data-bs-toggle="modal" data-bs-target="#featureInsertModal">
                                    @SharedLocalizer.Static["Insert"] <img src="/images/toolbar/toolbar_Dark_Insert.png" width="16" height="16" alt="@SharedLocalizer.Static["Insert"]...">
                                </button>
                            </div>
                        }
                        else
                        {
                            <div class="d-flex gap-2 mb-2">
                                <button type="button" class="btn btn-sm btn-light p-1" onclick="surroundSelectionWith('**','**')">
                                    <img src="/images/toolbar/toolbar_Light_Bold.png" width="16" height="16" alt="@SharedLocalizer.Static["Bold"]">
                                </button>
                                <button type="button" class="btn btn-sm btn-light p-1" onclick="surroundSelectionWith('//','//')">
                                    <img src="/images/toolbar/toolbar_Light_Italic.png" width="16" height="16" alt="@SharedLocalizer.Static["Italic"]">
                                </button>
                                <button type="button" class="btn btn-sm btn-light p-1" onclick="surroundSelectionWith('__','__')">
                                    <img src="/images/toolbar/toolbar_Light_Underline.png" width="16" height="16" alt="@SharedLocalizer.Static["Underline"]">
                                </button>
                                <button type="button" class="btn btn-sm btn-light p-1" onclick="surroundSelectionWith('~~','~~')">
                                    <img src="/images/toolbar/toolbar_Light_Strike.png" width="16" height="16" alt="@SharedLocalizer.Static["Strikethrough"]">
                                </button>
                                <button type="button" class="btn btn-sm btn-light p-1" onclick="surroundSelectionWith('!!','!!')">
                                    <img src="/images/toolbar/toolbar_Light_Highlight.png" width="16" height="16" alt="@SharedLocalizer.Static["Highlight"]">
                                </button>
                                <button type="button" class="btn btn-sm btn-light d-flex align-items-center gap-1 px-2" data-bs-toggle="modal" data-bs-target="#insertLinkModal">
                                    <img src="/images/toolbar/toolbar_Light_Link.png" width="16" height="16" alt="@SharedLocalizer.Static["Hyperlink"]...">
                                </button>
                                <button type="button" class="btn btn-sm btn-light d-flex align-items-center gap-1 px-2" data-bs-toggle="modal" data-bs-target="#insertEmojiModal">
                                    <img src="/images/toolbar/toolbar_Light_Emoji.png" width="16" height="16" alt="@SharedLocalizer.Static["Emoji"]...">
                                </button>

                                <div style="width:1px; height:24px; background-color:#222;"></div>

                                <button type="button" class="btn btn-sm btn-light d-flex align-items-center gap-1 px-2" data-bs-toggle="modal" data-bs-target="#featureInsertModal">
                                    @SharedLocalizer.Static["Insert"] <img src="/images/toolbar/toolbar_Light_Insert.png" width="16" height="16" alt="@SharedLocalizer.Static["Insert"]...">
                                </button>
                            </div>
                        }

                        <textarea id="Body" name="Body" spellcheck="true">@(Model?.Body ?? String.Empty)</textarea>
                    </div>

                    <div class="tab-pane fade"
                         id="preview-tab-pane"
                         role="tabpanel"
                         aria-labelledby="preview-tab"
                         tabindex="0">

                        <div id="previewLoading" class="py-3 d-none">
                            <div class="spinner-border spinner-border-sm me-2" role="status"></div>
                            @SharedLocalizer.Static["Loading preview..."]
                        </div>

                        <div id="previewError" class="alert alert-danger d-none mt-3"></div>

                        <div id="previewContent" class="border rounded p-3 mt-3 bg-body">
                            <em>@SharedLocalizer.Static["Please wait, loading preview..."]</em>
                        </div>
                    </div>
                </div>

                <!-- EDITOR END -->
                <br />

                <div class="form-group">
                    <button type="submit" class="btn btn-primary">@SharedLocalizer.Static["Save"]</button>
                    @if (Model?.Id > 0 && sessionState.HoldsPermission(WikiPermission.Read))
                    {
                        <a href="@GlobalConfiguration.BasePath/@sessionState.PageNavigation" target="_blank" rel="noopener" class="btn btn-success" role="button">@SharedLocalizer.Static["View"]</a>
                        <a href="@GlobalConfiguration.BasePath/@sessionState.PageNavigation/Export" target="_blank" rel="noopener" class="btn btn-secondary" role="button">@SharedLocalizer.Static["Export"]</a>
                    }
                    <span class="badge text-bg-danger invisible">@SharedLocalizer.Static["Unauthorized"]</span>
                </div>
            }
        </div>
    </div>
    <br />

    <div class="card shadow-lg">
        <div class="card-header"><h3>@SharedLocalizer.Static["Attachments"]</h3></div>
        <div class="card-body">
            @if (Model?.Id > 0 && sessionState.HoldsPermission(WikiPermission.Create))
            {
                <div id="dropSection" class="dropSection">
                    <span class="d-inline-block">@SharedLocalizer.Static["Drop file attachments here or click to upload manually."]</span>
                </div>
                <input type="file" id="fileInput" style="display:none" multiple>
                <div id="uploadStatus"></div>
                <div id="uploadedFiles"></div>
            }
            else
            {
                <div>@SharedLocalizer.Static["Save the page before uploading files."]</div>
            }
        </div>
    </div>
</div>

<div class="modal" tabindex="-1" id="templatesModal" aria-labelledby="templatesModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="templatesModalLabel">@SharedLocalizer.Static["Choose template"]</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            @if (Model?.Templates.Any() ?? false)
            {
                <div class="list-group list-group-flush">
                    @foreach (var template in Model.Templates)
                    {
                        <a href="#" data-tmpurl="@GlobalConfiguration.BasePath/Page/Template/@template.Id" class="list-group-item list-group-item-action js_use-template" aria-current="true">
                            <div class="d-flex w-100 justify-content-between">
                                <h5 class="mb-1">@template.Title</h5>
                                <small>@template.CreatedByUserName</small>
                            </div>
                            <p class="mb-1">@template.Description</p>
                        </a>
                    }
                </div>
            }

            <div class="modal-footer">
                <div class="container-fluid">
                    <em class="small">* - @SharedLocalizer.Static["Use the Templates namespace to add a page to the list of templates, e.g. Templates :: My First Template."]</em>
                    <button type="button" class="btn btn-secondary float-end" data-bs-dismiss="modal">@SharedLocalizer.Static["Close"]</button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal" tabindex="-1" id="featureInsertModal" aria-labelledby="featureInsertModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="featureInsertModalLabel">@SharedLocalizer.Static["Insert"]</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="@SharedLocalizer.Static["Close"]"></button>
            </div>

            <div class="modal-body p-0">
                @* Search *@
                <div class="p-3 border-bottom position-sticky top-0" style="z-index:1">
                    <div class="input-group input-group-sm">
                        <span class="input-group-text" id="featureInsertSearchLabel">🔎</span>
                        <input id="featureInsertModalSearch" type="text" class="form-control" placeholder="@SharedLocalizer.Static["Search"]..." aria-labelledby="featureInsertSearchLabel">
                    </div>
                </div>
                @* Table *@
                <div class="p-3">
                    <div id="featureInsertModalTable"></div>
                </div>
            </div>

            <div class="modal-footer">
                <div class="container-fluid">
                    <em class="small"></em>
                    <button type="button" class="btn btn-secondary float-end" data-bs-dismiss="modal">@SharedLocalizer.Static["Close"]</button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal" tabindex="-1" id="insertLinkModal" aria-labelledby="insertLinkModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="insertLinkModalLabel">@SharedLocalizer.Static["Hyperlink"]</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="@SharedLocalizer.Static["Close"]"></button>
            </div>

            <div class="modal-body p-3">
                <div class="mb-3">
                    <label class="form-label fw-bold">@SharedLocalizer.Static["Link Type"]</label><br />
                    <div class="form-check form-check-inline">
                        <input class="form-check-input" type="radio" name="linkMode" id="linkModePage" value="page" checked>
                        <label class="form-check-label" for="linkModePage">@SharedLocalizer.Static["Wiki Page"]</label>
                    </div>
                    <div class="form-check form-check-inline">
                        <input class="form-check-input" type="radio" name="linkMode" id="linkModeUrl" value="url">
                        <label class="form-check-label" for="linkModeUrl">@SharedLocalizer.Static["External URL"]</label>
                    </div>
                </div>

                <div>
                    <label for="insertLinkModalLinkTitle" class="form-label fw-bold">@SharedLocalizer.Static["Link Text (Optional)"]</label>
                    <input id="insertLinkModalLinkTitle" type="url" class="form-control" placeholder="@SharedLocalizer.Static["Link Text (Optional)"]" />
                </div>

                <div id="linkInternalDiv">
                    <label for="insertLinkModalLinkPage" class="form-label fw-bold">@SharedLocalizer.Static["Choose a page"]</label>
                    <select id="insertLinkModalLinkPage" style="width: 100%"></select>
                </div>

                <div id="linkExternalDiv" style="display: none;">
                    <label for="insertLinkModalLinkURL" class="form-label fw-bold">@SharedLocalizer.Static["External URL"]</label>
                    <input id="insertLinkModalLinkURL" type="url" class="form-control" placeholder="https://example.com" />
                </div>
            </div>

            <div class="modal-footer">
                <div class="container-fluid">
                    <em class="small"></em>
                    <button type="button" class="btn btn-success float-end" id="insertLinkModalInsertButton">@SharedLocalizer.Static["Insert"]</button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal" tabindex="-1" id="insertEmojiModal" aria-labelledby="insertEmojiModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="insertEmojiModalLabel">@SharedLocalizer.Static["Insert Emoji"]</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="@SharedLocalizer.Static["Close"]"></button>
            </div>

            <div class="modal-body p-3">
                <div class="d-flex align-items-center gap-2 mb-2">
                    <input id="insertEmojiModalSearchText" type="text" class="form-control" placeholder="Search for more emojis.">
                    <button id="insertEmojiModalSearchButton" type="button" class="btn btn-outline-secondary">Search</button>
                </div>

                <div id="emojiResultsTable" class="border rounded mb-2" style="max-height: 360px;"></div>
                <a href="@GlobalConfiguration.BasePath/Wiki_Help::List_Of_All_Emojis" target="_blank" rel="noopener noreferrer">Having trouble finding an emoji? View them all.</a>
            </div>
        </div>
    </div>
</div>
