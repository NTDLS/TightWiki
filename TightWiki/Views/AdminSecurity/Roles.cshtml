@using TightWiki
@using TightWiki.Plugin
@using TightWiki.Plugin.Interfaces
@using TightWiki.Plugin.Library
@using TightWiki.ViewModels.AdminSecurity
@inject ITwSharedLocalizationText Localizer
@inject TwConfiguration WikiConfiguration

@model RolesViewModel
@{
    Layout = "_Layout";
    var sessionState = ViewData["SessionState"] as TightWiki.TwSessionState ?? throw new Exception("Wiki State Context cannot be null.");
}

<div class="d-flex justify-content-between align-items-start flex-wrap gap-2 mb-3">
    <div>
        <h3 class="mb-1">
            @Localizer["Roles"]
        </h3>
        <div class="text-muted">
            @Localizer["Global configuration for security roles and membership."]
        </div>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-header d-flex justify-content-between align-items-center flex-wrap gap-2">
        <span class="fw-semibold">
            <i class="bi bi-shield-lock me-2"></i>@Localizer["Security Roles"]
        </span>

        <a href="@WikiConfiguration.BasePath/AdminSecurity/AddRole"
               class="btn btn-primary">
            <i class="bi bi-plus-circle me-1"></i>@Localizer["Add Role"]
        </a>
    </div>

    @if (Model.Roles.Count > 0)
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th scope="col" style="width: 30%;">
                            <a class="link-dark text-decoration-none"
                                   href="?@TwQueryStringConverter.OrderHelper(sessionState, "Name")">
                                @Localizer["Name"]
                            </a>
                        </th>
                        <th scope="col">
                            <a class="link-dark text-decoration-none"
                                   href="?@TwQueryStringConverter.OrderHelper(sessionState, "Description")">
                                @Localizer["Description"]
                            </a>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var role in Model.Roles)
                    {
                        <tr>
                            <td>
                                <a class="text-decoration-none fw-semibold"
                                       href="@WikiConfiguration.BasePath/AdminSecurity/Role/@role.Name">
                                    @role.Name
                                </a>
                            </td>
                            <td>
                                @(string.IsNullOrWhiteSpace(role.Description)
                                    ? Localizer["(No description)"].Value
                                    : role.Description)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="card-body text-muted">
            <i class="bi bi-shield-x me-2"></i>
            @Localizer["There are no security roles configured."]
        </div>
    }
</div>