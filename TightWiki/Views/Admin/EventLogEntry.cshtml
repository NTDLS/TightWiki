@using TightWiki.Models

@using TightWiki.Resources

@model TightWiki.Models.ViewModels.Admin.EventLogEntryViewModel
@{
    Layout = "_Layout";
    var sessionState = ViewData["SessionState"] as TightWiki.SessionState ?? throw new Exception("Wiki State Context cannot be null.");
}

<h3>
    @SharedLocalizer.Static["Event Log Entry"]
</h3>

<div class="card shadow-sm">
    <div class="card-header fw-semibold">
        @SharedLocalizer.Static["Log Entry Details"]
    </div>

    <div class="table-responsive">
        <table class="table table-bordered table-striped mb-0 align-middle">
            <tbody>

                <tr>
                    <th class="w-25">@SharedLocalizer.Static["Id"]</th>
                    <td>@Model.LogEntry.Id</td>
                </tr>

                <tr>
                    <th>@SharedLocalizer.Static["Severity"]</th>
                    <td>
                        @{
                            string severity = Model.LogEntry.Severity ?? "";
                            string icon = "bi-circle";
                            string badge = "border-secondary text-secondary bg-light";

                            if (severity.Equals("Verbose", StringComparison.OrdinalIgnoreCase))
                            {
                                icon = "bi-terminal";
                                badge = "border-secondary text-secondary bg-light";
                            }
                            else if (severity.Equals("Information", StringComparison.OrdinalIgnoreCase))
                            {
                                icon = "bi-info-circle-fill";
                                badge = "border-info text-info bg-info-light";
                            }
                            else if (severity.Equals("Warning", StringComparison.OrdinalIgnoreCase))
                            {
                                icon = "bi-exclamation-triangle-fill";
                                badge = "border-warning text-warning bg-light";
                            }
                            else if (severity.Equals("Error", StringComparison.OrdinalIgnoreCase))
                            {
                                icon = "bi-x-circle-fill";
                                badge = "border-danger text-danger bg-light";
                            }
                        }

                        <span class="badge border @badge">
                            <i class="bi @icon me-1"></i>
                            @severity
                        </span>
                    </td>
                </tr>

                <tr>
                    <th>@SharedLocalizer.Static["Date/Time"]</th>
                    <td>@Model.LogEntry.CreatedDate</td>
                </tr>

                @if (!string.IsNullOrEmpty(Model.LogEntry.Text))
                {
                    <tr>
                        <th>@SharedLocalizer.Static["Text"]</th>
                        <td class="text-break">@Model.LogEntry.Text</td>
                    </tr>
                }

                @if (!string.IsNullOrEmpty(Model.LogEntry.ExceptionText))
                {
                    <tr>
                        <th>@SharedLocalizer.Static["Exception"]</th>
                        <td class="text-danger text-break">
                            @Model.LogEntry.ExceptionText
                        </td>
                    </tr>
                }

                @if (!string.IsNullOrEmpty(Model.LogEntry.StackTrace))
                {
                    <tr>
                        <th>@SharedLocalizer.Static["Stack Trace"]</th>
                        <td>
                            <pre class="mb-0 small bg-light p-2 rounded border"
                                 style="white-space: pre-wrap; max-height: 300px; overflow-y: auto;">
                                @Model.LogEntry.StackTrace
                            </pre>
                        </td>
                    </tr>
                }

            </tbody>
        </table>
    </div>
</div>

