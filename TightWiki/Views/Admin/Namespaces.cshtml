@model TightWiki.Models.ViewModels.Admin.NamespacesViewModel
@using TightWiki.Plugin
@using TightWiki.Plugin.Interfaces
@using TightWiki
@using TightWiki.Library
@using TightWiki.Models
@inject ISharedLocalizationText Localizer
@inject TwConfiguration WikiConfiguration

@{
    Layout = "_Layout";
    var sessionState = ViewData["SessionState"] as TightWiki.SessionState ?? throw new Exception("Wiki State Context cannot be null.");
}

<div class="d-flex justify-content-between align-items-start flex-wrap gap-2 mb-3">
    <div>
        <h3 class="mb-1">
            @Localizer["Namespaces"]
        </h3>
        <div class="text-muted">
            @Localizer["All namespaces contained in the wiki."]
        </div>
    </div>
</div>

@if (Model.Namespaces.Count > 0)
{
    <div class="card shadow-sm">
        <div class="card-body pb-0">
            @TightWiki.Library.PageSelectorGenerator.Generate(Context.Request.QueryString, Model.PaginationPageCount, @class: "mb-3")
        </div>

        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th scope="col">
                            <a class="link-dark text-decoration-none"
                                   href="?@QueryStringConverter.OrderHelper(sessionState, "Name")">
                                @Localizer["Name"]
                            </a>
                        </th>
                        <th scope="col" class="text-end text-nowrap" style="width: 1%;">
                            <a class="link-dark text-decoration-none"
                                   href="?@QueryStringConverter.OrderHelper(sessionState, "Pages")">
                                @Localizer["Pages"]
                            </a>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var p in Model.Namespaces)
                    {
                        var namespaceName = string.IsNullOrEmpty(p.Namespace) ? "(Default)" : p.Namespace;

                        <tr>
                            <td>
                                <a class="text-decoration-none fw-semibold"
                                       href="@WikiConfiguration.BasePath/Admin/Namespace/@(p.Namespace ?? string.Empty)">
                                    @namespaceName
                                </a>
                            </td>
                            <td class="text-end text-nowrap">
                                @p.CountOfPages.ToString("N0")
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="card-body pt-3">
            @TightWiki.Library.PageSelectorGenerator.Generate(Context.Request.QueryString, Model.PaginationPageCount)
        </div>
    </div>
}
else
{
    <div class="card shadow-sm">
        <div class="card-body text-muted">
            <i class="bi bi-folder2-open me-2"></i>
            @Localizer["There are currently no pages which exist within namespaces."]
        </div>
    </div>
}