@model TightWiki.Models.ViewModels.Admin.DeletedPagesRevisionsViewModel
@using TightWiki.Library
@using TightWiki.Models

@using TightWiki.Resources

@{
    Layout = "_Layout";
    var sessionState = ViewData["SessionState"] as TightWiki.SessionState ?? throw new Exception("Wiki State Context cannot be null.");
}

<div class="d-flex justify-content-between align-items-start flex-wrap gap-2 mb-3">
    <div>
        <h3 class="mb-1">
            @SharedLocalizer.Static["Deleted Page Revisions"]
        </h3>
        <div class="text-muted">
            @SharedLocalizer.Static["Page revisions that have been deleted for the given page. These can be purged or restored."]
        </div>
    </div>
</div>

<div class="card shadow-sm mb-3">
    <div class="card-header d-flex justify-content-between align-items-center flex-wrap gap-2">
        <span class="fw-semibold">
            <i class="bi bi-clock-history me-2"></i>@SharedLocalizer.Static["Deleted Revisions"]
        </span>

        @TightWiki.Library.ConfirmActionHelper.GenerateDangerButton(
            GlobalConfiguration.BasePath,
            SharedLocalizer.Static["This will permanently purge all deleted page revisions for \"{0}\". Continue?"].Format(Model.Name),
            SharedLocalizer.Static["Purge Deleted Revisions"].Value,
            $"/Admin/PurgeDeletedPageRevisions/{Model.PageId}",
            Context.Request.Path.Value)
    </div>
</div>

@if (Model.Revisions.Count > 0)
{
    <div class="card shadow-sm">
        <div class="card-body pb-0">
            @TightWiki.Library.PageSelectorGenerator.Generate(Context.Request.QueryString, Model.PaginationPageCount)
        </div>

        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th scope="col" class="text-nowrap">
                            <a class="link-dark text-decoration-none"
                                   href="?@QueryStringConverter.OrderHelper(sessionState, "Revision")">
                                @SharedLocalizer.Static["Revision"]
                            </a>
                        </th>
                        <th scope="col" class="text-nowrap">
                            <a class="link-dark text-decoration-none"
                                   href="?@QueryStringConverter.OrderHelper(sessionState, "DeletedDate")">
                                @SharedLocalizer.Static["Deleted Date"]
                            </a>
                        </th>
                        <th scope="col">
                            <a class="link-dark text-decoration-none"
                                   href="?@QueryStringConverter.OrderHelper(sessionState, "DeletedBy")">
                                @SharedLocalizer.Static["Deleted By"]
                            </a>
                        </th>
                        <th scope="col" class="text-nowrap" style="width: 1%;">
                            @SharedLocalizer.Static["Action"]
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var p in Model.Revisions)
                    {
                        <tr>
                            <td class="text-nowrap">
                                <a class="text-decoration-none fw-semibold"
                                       href="@GlobalConfiguration.BasePath/Admin/DeletedPageRevision/@p.Id/@p.Revision">
                                    @p.Revision
                                </a>
                            </td>

                            <td class="text-nowrap text-muted">
                                @p.DeletedDate
                            </td>

                            <td>
                                @p.DeletedByUserName
                            </td>

                            <td class="text-nowrap">
                                <div class="d-flex gap-2">
                                    @TightWiki.Library.ConfirmActionHelper.GenerateSafeButton(
                                        GlobalConfiguration.BasePath,
                                        SharedLocalizer.Static["This will restore the deleted revision. Continue?"].Value,
                                        SharedLocalizer.Static["Restore"].Value,
                                        $"/Admin/RestoreDeletedPageRevision/{p.Id}/{p.Revision}",
                                        Context.Request.Path.Value,
                                        @class: "btn btn-success btn-sm d-inline-flex align-items-center")

                                    @TightWiki.Library.ConfirmActionHelper.GenerateDangerButton(
                                        GlobalConfiguration.BasePath,
                                        SharedLocalizer.Static["This will permanently delete the specified deleted revision. Continue?"].Value,
                                        SharedLocalizer.Static["Purge"].Value,
                                        $"/Admin/PurgeDeletedPageRevision/{p.Id}/{p.Revision}",
                                        Context.Request.Path.Value,
                                        @class: "btn btn-danger btn-sm d-inline-flex align-items-center")
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="card-body pt-3">
            @TightWiki.Library.PageSelectorGenerator.Generate(Context.Request.QueryString, Model.PaginationPageCount)
        </div>
    </div>
}
else
{
    <div class="card shadow-sm">
        <div class="card-body text-muted">
            <i class="bi bi-inbox me-2"></i>
            @SharedLocalizer.Static["There are no deleted revisions for this page."]
        </div>
    </div>
}